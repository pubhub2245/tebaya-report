# 経理パッケージ：2店舗目（最初のお客さん）は本当に使えるのか — コードの実測

作成：2026-09-18 21:40 JST（司令室B・自動作成）／対象：`pubhub2245/tebaya-report`
読む人：じゅん（技術用語には説明を付けています）

## 0. 結論（先に3行）

- **いまのままでは、お金を払ったお店は使い始められません。**
- 理由は3つあり、どれも「このアプリが手羽屋1店舗だけを前提に作られている」ことから来ています。
- **送る（kp24）より先に、ここを決める必要があります。** 送ってしまうと、払った店主が
  手羽屋の売上を自分の画面で見ることになり、返金と信用の両方を失います。

## 1. 何を確かめたか

紹介ページ（`/keiri/case`）が約束しているのは次の3つです。

1. 日報を打つ（`/report`）
2. 経理画面（`/keiri`）で「今月の利益・今の現金・まだ払っていないお金」を見る
3. 会計ソフト用のCSVを出す

この3つを、**申し込んだ2店舗目の立場で**コードを1行ずつ追って確かめました。
本番のデータは1件も読んでいません（コードを読んだだけです）。

## 2. 見つかったこと（3つ）

### ① 経理画面は「手羽屋の設定」しか読まない

`app/keiri/page.tsx` の56行目に `const BUSINESS_CODE = "tebaya";` と書いてあり、
設定（数え始めの日・その日の現金・外注費の率）も、支払いの記録も、
**手羽屋の行だけ**を読み書きしています。

いっぽう、申し込んだお店の設定は `t_<お店の番号>` という別のコードで保存されます
（`lib/keiri/tenants.ts` の `tenantBusinessCode`）。
つまり **初回設定で入れてもらう3項目は、保存はされるが、どの画面からも使われません。**

### ② 日報に「どの店の日報か」が無い

経理画面が読む日報は `keiri_reports`（＝日報から写真を抜いた見え方だけの棚）ですが、
ここには**お店で絞る列がありません**（`supabase/migrations/keiri_settings_and_payments.sql`）。
元の `daily_reports` にも「どの店か」の列はありません。

結果、2店舗目が日報を打つと：

- その日報は手羽屋の日報と同じ棚に入る
- 2店舗目の経理画面には**手羽屋の売上が自分の売上として**出る
- 手羽屋の画面にも、2店舗目の日報が混ざる

さらに日報の選択肢（出店場所・スタッフ・商品の単価）も手羽屋のマスタなので、
2店舗目は「ながやま三股」「イデ」といった手羽屋の中身を選ぶことになります。

### ③ 初回設定で渡す合言葉は、どこにも通用しない

初回設定（`/keiri/welcome`）は最後に「管理画面に入るための合言葉」を1回だけ出し、
「控えてください」と伝えます。この合言葉は戻せない形（ハッシュ）で保存されます。

ところが、管理画面の入口（`app/components/AdminGate.tsx` → `lib/adminPassword.ts`）は
**Vercel に入れてある1つの合言葉（手羽屋用）としか比べていません**。
保存したハッシュを見に行くコードは、アプリのどこにもありません（実測：参照0件）。

つまり、払った店主は言われたとおり控えた合言葉で `/keiri` を開こうとして、
**必ずはじかれます**。しかも仮に開けたら手羽屋のお金が見えてしまいます。

## 3. なぜ今まで気づかなかったか

設計書（`docs/auto/2026-09-17_経理パッケージ_無人販売の流れ_設計.md` の4章末尾）には
「`keiri_tenants.id` を `keiri_settings` と `daily_reports` に持たせる必要がある」と
書いてありました。**`keiri_settings` には足しましたが、`daily_reports` の方が残っています。**
申し込みの受け皿（お店の行を作る・初回設定を保存する）は動くので、
受け皿の診断（`/api/keiri/diagnose`）は ready: true になります。
**「申し込めるか」と「使えるか」は別**で、いま欠けているのは後者です。

## 4. 進み方の案（じゅんの「はい」が要ります）

| 案 | 中身 | じゅんの手間 | 費用 | 向き不向き |
|---|---|---|---|---|
| **A（おすすめ）** | 日報に「どの店か」の印を1つ足し、日報・経理・管理の画面をその店のぶんだけに絞る。合言葉の確認もサーバー側でお店ごとに行う | SQLを1回貼る（kp27と同じ作業） | 0円 | 無人で売る形に一番近い。ただし**手羽屋が毎日使っている画面に手を入れる**ので、作業後に手羽屋側が今までどおり動くことを確かめる必要がある |
| B | お店ごとに別の入れ物（別のVercel・別のSupabase）を用意する | 1件ごとに画面操作が要る | 無料枠を超えると課金 | 原則「じゅんの作業ゼロ」に反する。件数が増えるほど重くなる |
| C | 当面は売らない（紹介ページの申し込みボタンを下ろし、無料の計算ツールだけ残す） | なし | 0円 | 最初の1件が遠のく。ただし「払わせて使えない」は起きない |

**Bの見立て：A。** 理由は3つ。
(1) 足すのは列1つと絞り込みだけで、手羽屋の既存の行はすべて「印なし＝手羽屋」として
今までどおり動かせる（消す・書き換える作業が無い）。
(2) Bは1件増えるごとにじゅんの作業が増えるので、原則2（じゅんの作業ゼロ）と合わない。
(3) Cは安全だが、最優先で決めた「最初の1件を取る」から遠ざかる。

**ただし A は、手羽屋の日報・レジ・シフトが毎日動いている最中の工事**です。
CLAUDE.md の「既存の日報の入力フロー・画面を変えない」に触れるため、
**Claudeの判断では着手しません。司令室の【要確認】で選んでもらいます。**

## 5. Aを選んだ場合に何をするか（先に見えるようにしておく）

1. 日報の棚に「どの店か」の印を足す（空＝手羽屋。既存の151件以上の行は1つも書き換えない）
2. 日報を保存するときに、その店の印を入れる（手羽屋の画面は印が空のまま＝今までどおり）
3. 経理画面・管理画面・レジ・集計を「自分の店の印のぶんだけ」に絞る
4. 合言葉の確認をサーバー側に移し、お店ごとの合言葉で入れるようにする
5. 手羽屋の画面が今までどおりであることを、テストと本番の見た目で確かめてから反映する

いずれも下書き（別のコピー）で作り、本番に出すのは じゅんの「はい」のあとです。

## 6. この文書でやっていないこと

- 本番のデータは読んでいません（コードだけを読みました）
- コードは1行も変えていません（この文書を足しただけです）
- 手羽屋の日報・シフト・レジ・LINE には触れていません
