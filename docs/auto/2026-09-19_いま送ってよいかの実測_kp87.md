# 「いま知り合いの店主に送ってよいか」の実測（kp87）

2026-09-19 15:34 B。本番（https://tebaya-report.vercel.app）を**外から**通しで歩いた結果。
数字を目で数えず、版の合言葉（`/api/version` の `build` と各ページの `<meta name="x-build">`）で
最新かどうかを先に確かめてから測っている（kp85 の決着どおり）。

## 結論（1行）

**送ってよい。ただし相手に「支払いのリンク」はまだ渡さない。**
理由：案内 → お試し → 申し込み → 送信後までは全部つながっていて、申し込みは
LINE とメールの2経路で必ず じゅん に届く。
つながっていないのは**お金を払ったあと**だけで、そこは今のフォームでは通らない
（フォームに「この画面ではお支払いは発生しません。お支払いの方法は、ご連絡のときにご案内します」と明記してある）。
＝ 申し込みを受けるところまでは完全に動くので、送るのを止める理由は無い。
　　支払いの案内だけは、鍵（kp55）と Stripe の通知先（kp27）が済んでから出す。

## 通しで歩いた結果（版はすべて `a680760@2026-09-19T05:45:58Z` ＝最新）

| 段 | URL | 結果 |
|---|---|---|
| ① 案内 | `/keiri/case` | **動く。** 本文2,847字・月額15,000円が12か所・お試し版と申し込みへの導線あり |
| ② お試し | `/keiri/demo` | **動く。** 本文2,648字・架空の店の数字（利益¥73,600／現金¥215,600）が出ている |
| ③ 申し込み | `/keiri/apply` | **動く。** 4項目のフォーム・価格表示・特商法表記あり |
| ④ 送信（入力もれ） | `POST /api/keiri/apply` | **400。** 「お店の名前を入れてください」等、直せる言葉で返る |
| ⑤ 送信（疎通確認） | `POST /api/keiri/apply` | **200。** 受け口は生きている（機械判定の道で確かめたので、誰にも知らせず・控えも作っていない） |
| ⑥ 申し込みの届き先 | `/api/keiri/diagnose` | **届く。** LINE は今月あと5通／メールの下書きは2か所（じゅん＋司令室） |
| ⑦ 払ったあとの初回設定 | `POST /api/keiri/welcome` | **止まる。** 「このリンクは使えません。申し込み完了の画面から開き直してください」 |
| ⑧ 合言葉での入室 | `POST /api/keiri/login` | **止まる。** 「パスワードが違います」（鍵が壊れていて0件が返るため） |

⑦⑧ が止まる原因は1つ（kp55：Vercel の `SUPABASE_SERVICE_ROLE_KEY` の貼り直し・2分）。
なお支払いボタンそのものも、まだサイトに出ていない（`NEXT_PUBLIC_KEIRI_PAYMENT_LINK_15000` 未設定・kp27）。
**つまり ⑦⑧ は、いまの送り方（じゅんが手で返信し、支払い方法を手で案内する）では誰も通らない。**

## 送るときに守ること（この1つだけ）

**返信に Stripe の支払いリンクを貼らない。**
先に払われると、戻り先（`/keiri/welcome`）で「このリンクは使えません」に当たって、
払ったばかりの店主が行き止まりになる。
申し込みが来たら、じゅんは「ありがとうございます、折り返します」だけ返せばよい。
支払いの案内は kp55（鍵）と kp27（Stripe の通知先）が済んでから。

## 途中で見つけて、この場で直したもの

1. **`/keiri` に迷い込んだ店主の行き止まり。** `/keiri` は経理の画面（合言葉つき）で、
   出口が「← トップに戻る」＝**手羽屋の業務メニュー**しか無かった。
   ご案内にもお試し版にもたどり着けず、よその店の人を手羽屋の内側へ案内していた。
   → 経理の入口だけ、出口を「ご案内を見る」「お試し版を触ってみる」に差し替えた。
2. **診断がうそをついていた2か所。**
   「訪問は読む許可がありません」→ 実際は数える窓口が動いている（`/api/hit/summary`）。
   「控えは残りません」→ 9/19 15:05 に棚を「入れるだけのポスト」にしたので残る。
   → 診断が実際に窓口を叩いて答えるようにし、断定できないことは断定しない言い方に直した。
3. **申し込みの棚の決まりが、倉庫にファイルとして残っていなかった。**
   A が手で流したままだったので、倉庫を作り直すと黙って消える形だった。
   → `supabase/migrations/keiri_applications_insert_only.sql` に書き写した。
