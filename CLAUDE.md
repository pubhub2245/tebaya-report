# 手羽屋 日報アプリ - プロジェクト説明書

## 1. プロジェクト概要

- **アプリ名**: 手羽屋 日報アプリ
- **目的**: 手羽屋（移動販売/出店形式の飲食事業）の営業日報・売上管理システム
- **運営**: 川畑潤一郎さん関連事業

## 2. 技術スタック

- **フレームワーク**: Next.js 14（App Router）
- **言語**: TypeScript
- **データベース**: Supabase（プロジェクトID: vtuyebyjbvjmucqpkxug）
- **ホスティング**: Vercel
- **本番URL**: https://tebaya-report.vercel.app
- **GitHub**: pubhub2245/tebaya-report

## 3. 実装済みの機能

- **トップメニュー**（/） - アプリの入口ページ
- **営業後日報**（/report） - 8ステップ形式で営業終了後の報告を入力
  - **売上の内訳チェック**（2026-08 追加）
    商品ごとの本数を入力し、「単価 × 本数」の合計がレジの売上と合うか確かめる。
    合わないと STEP4 から先へ進めない（理由を選べば進める。理由も日報に残る）。
    それ以前は手羽先の本数を入力せず `(売上 − ほかの商品) ÷ 200` で逆算していたため、
    内訳が売上から作られた数字になっており検算になっていなかった。
    さらに切り捨てのぶん（0〜199円）が毎日消え、手羽先が0〜1本ぶん少なく記録されていた。
    単価は商品マスタ（sale_products）から取り、その日に使った単価を
    日報の `product_prices` に控えとして保存する（あとで値上げしても過去が壊れない）。
  - **STEP5 は「レジから払った経費」**（2026-08 改名。旧「立替経費」）
    レジのお金から払った経費だけを入れる欄。入力するとその日の手元現金からすぐ引かれる。
    自分のお金で立て替えた分は入れない（→ `/keiri/advances`）。
    **改名しただけで、入力項目・ステップ数・計算・保存先は一切変えていない。**
- **中間報告**（/interim） - 営業中の途中経過を報告
- **管理者ページ**（/admin）
  - 月次集計（月ごとの売上まとめ）
  - 日報一覧（過去の日報を確認）
  - 日報削除機能
- **レシートOCR自動入力機能** - レシート写真から自動で数字を読み取り
- **レジ差異金額入力** - レジのお金の過不足を記録
- **2回連続マイナス補填アラート** - レジが2回続けてマイナスのとき警告
- **持ち帰り金額集計** - テイクアウト売上の集計
- **月間売上目標vs実績ダッシュボード** - 目標と実績の比較表示

> ※ 以前ここに「タスク管理（/tasks）」と書かれていたが、**画面は存在しない**（テーブル
> `tasks` に6件・最終2026-04-16）。実態に合わせて記載を削除した（2026-08-26）。
> 使っていない／止まっている機能：仕込み日報（2026-06-09で停止）、キャンセル記録（2026-06-04で停止）。
> 再開するか畳むかは未決定。畳むならトップページの導線も外すこと。

## 4. 土台のルール（お金・鍵・控え）

2026-08 に外部レビューを行い、そこで直した部分。**ここを崩さないこと。**

### 4-1. お金の計算は `lib/money.ts` だけに書く

粗利の「食材25%・場代10%」や経費の合計が、画面ごとにコピーされていた
（同じ計算が3か所）。片方だけ直すと画面によって金額が違う事故になるので、
**すべて `lib/money.ts` に集約**し、`tests/money.test.ts` で固定した。

- 金額の計算を足す・変えるときは、必ず `lib/money.ts` に書いてテストを足す。
- 画面の中で `sales * 0.25` のような計算を直接書かない。
- `npm test` が通らない変更は本番に入れない。

**推定と実績は別物**：`calcGrossProfit` は推定（食材25%・場代10%の見込み）、
`calcActualProfit` は実績（実際にレジから払った経費）。管理者ページは両方を並べて表示する。
※ 実データでは場代の実績は売上の約6.9%で、推定の10%とはズレる。

### 4-2. 集計画面は経費の明細を取得しない

昔の日報はレシート写真が直接入っていて非常に重い（写真つき12件で18MB）。
※ 新しい写真は置き場に移す作りにした（→ 4-7）が、**このルールは引き続き守ること**。
集計に必要なのは合計だけなので、`daily_reports.expenses_total`（トリガーで自動計算）を使う。

- 集計画面（`/cash` `/admin` `/sales-report`）は `expenses_total` を select する。
- 明細（`expenses`）を select してよいのは、**その1日ぶんを表示するときだけ**。
- 合計は `expensesTotalOf()` を使う（列が無ければ明細から足す作りになっている）。

### 4-3. 日当はスタッフマスタが正

`staff_members.daily_wage` が正。`lib/formState.ts` の `STAFF_DAILY_PAY` は
**マスタに値が無い人のための保険**でしかない。取り出しは `lib/staffWage.ts` を使う。
日当を変えるときは管理者ページのスタッフマスタを直す（コードは触らない）。

### 4-4. 管理者パスワードをコードに書かない

- 直書きの初期値は削除済み。`NEXT_PUBLIC_ADMIN_PASSWORD` が未設定なら
  **誰も管理画面に入れない**（設定を促す画面が出る）。開けっぱなしにはしない。
- 判定は `lib/adminPassword.ts` の `checkAdminPassword()` だけを使う。
- **未解決**：`NEXT_PUBLIC_` で始まる設定はブラウザに配られるファイルに埋め込まれるため、
  パスワードは技術的には読めてしまう。根本対策は「合言葉の確認をサーバー側で行う」作りに
  変えること（次の段階）。

### 4-5. バックアップは毎日自動で取る

`lib/backup.ts` の `runBackup()` を、毎日の cron
（`/api/cron/calculate-achievement-rates`）から呼んでいる。

- Vercel の cron は本数に上限があるため、専用の枠を増やさず相乗りさせている。
- 容量が溢れないよう、控えは**直近5日ぶんだけ**残して古いものは自動で消える。
  自動で消すのは毎日のバックアップ（`CRITICAL_TABLES`）の分だけ。
  作業前に手で取った控え（例：`receipt_migration_backup`）は**消さない**（元に戻す命綱のため）。
- `SUPABASE_SERVICE_ROLE_KEY` が未設定だとバックアップは動かない。

### 4-7. レシート写真は「置き場」に置く（記録に埋め込まない）

2026-08 追加。写真そのものを日報の中に文字列（`data:image/jpeg;base64,...`）で
持たせるのをやめ、**Supabase Storage の `receipts` バケット**に置いて、
記録には**住所（URL）だけ**を持たせる。

- 埋め込みのままだと写真つき12件で18MBあり、日報1件を開くのも毎日の控えも重くなる。
- 新しく撮る写真は `lib/receiptStorage.ts` の `uploadReceiptOrKeep()` を通す。
  **置き場に入れられなかったときは、これまで通り埋め込み形式のまま保存する**
  （写真が原因で日報そのものが保存できなくなるのを防ぐ。あとから移し直せる）。
- 昔の記録の引っ越しは管理者ページの「🖼 写真を置き場へ移す」ボタンから。
  中身は `lib/receiptMigration.ts` ＋ `/api/admin/migrate-receipts`。
  **書き換える前に必ず控えを残す。残せなければ実行しない。**
  何度実行しても引っ越し済みの写真には触れない（途中で止まっても続きから進む）。
- 表示側は `<img src>` にそのまま渡すだけ。**埋め込み・URL のどちらでも映る**ので、
  昔の記録を残したままでも壊れない。
- `receipts` は公開バケット。**置く許可（INSERT）だけ**を与えており、
  一覧取得・上書き・削除はブラウザからできない。1枚5MBまで・画像のみ。

### 4-6. RLS（テーブルの鍵）

全テーブルで RLS を有効化済み。ただし**ポリシーは今のところ「全員OK」**で、
実質的な制限にはなっていない（ブラウザから直接読み書きする作りのため）。
本当に締めるには、書き込みをサーバー側に寄せる必要がある（次の段階）。
`table_snapshots`（バックアップ）だけはポリシー無し＝service_role のみで正しい状態。

## 5. 経理層（けいりそう）

2026-08 追加。日報のデータを、会計ソフトが読める形（仕訳）に変えるための土台。

### 5-1. 3層設計（この順番を崩さないこと）

| 層 | 役割 | ルール |
|---|---|---|
| **現場層** | スタッフの入力。日報・レジ締め・シフトなど | **今まで通り。既存の入力フローは一切変えない** |
| **変換層** | 「業態別の勘定科目マッピングテーブル」で日報データを仕訳データに変換する | 辞書は `keiri_account_mapping`。変換処理の実装は次ステージ |
| **出口層** | マネーフォワード クラウド会計の仕訳インポートCSVと試算表を出力する | 次ステージ以降 |

※「仕訳（しわけ）」＝会計ソフトが読む1行の記録。
「8月1日／売上高／12,000円／現金」のような形のこと。

### 5-2. 絶対に守る方針

- **会計ソフトそのものは作らない。**
  freee・マネーフォワードの競合ではなく「連携先」という位置づけ。
  出口はマネーフォワード クラウド会計の**仕訳インポートCSV**。
- **このアプリは税務判断をしない・させない。**
  `keiri_account_mapping` に入っている勘定科目・税区分は
  **税理士に見てもらうための叩き台（下書き）**であり、最終確定は必ず税理士のレビューで行う。
  理由：税務判断は税理士の独占業務であり、このアプリの提供範囲は「記帳の効率化」までだから。
  判断が割れるものには `needs_tax_advisor_review`（要税理士確認）フラグを立てる。
- **既存の日報の入力フロー・画面を変えない。**
  現場の入力負担を増やさないのが3層設計の大前提。
  経理層で項目を足したくなったら、勝手に足さずに必ず相談する。
- **`keiri_` で始まるテーブルが経理層。**
  経理層のテーブルは必ず `keiri_` プレフィックスを付ける。既存テーブルの変更・削除はしない（追加のみ）。

### 5-3. 現在あるもの

| もの | 場所 | 中身 |
|---|---|---|
| 入り口の設計メモ | `docs/keiri/advance-entrances.md` | 経費・立替の3つの箱の整理と、残課題（現場の立替の返金が現金残高に反映されない） |
| 現状調査メモ | `docs/keiri/survey.md` | 売上・経費・現金残高がどのテーブルのどこにあるか／変換に使えるデータと足りないデータ |
| 勘定科目マッピング | `keiri_account_mapping` テーブル | 業態コード・source_type・勘定科目・補助科目・税区分・貸借・要税理士確認フラグ。手羽屋テンプレ12件を投入済み（うち「売上・キャッシュレス」は `is_active = false`。下記参照） |
| 立替経費フォーム | `/keiri/advances`（`app/keiri/advances/page.tsx`） | 日付／立替した人／金額／種類／メモ の**5項目**＋レシート写真（任意）。保存先は `keiri_advance_expenses` |

### 5-4. 経費・立替の入り口（3つの箱・混ぜないこと）

詳しい経緯と残課題は `docs/keiri/advance-entrances.md`。

| 何の記録か | どこに入れる | 誰が使う | 手元現金 | 保存先 |
|---|---|---|---|---|
| レジのお金から払った経費 | 日報 STEP5「レジから払った経費」 | 現場スタッフ | すぐ減る | `daily_reports.expenses` |
| 自分のお金で立て替えた分（現場） | `/keiri/advances` | 現場スタッフ | 減らない | `keiri_advance_expenses` |
| 自分のお金で立て替えた分（経営） | `/cash/advances` | 経営側 | 精算時に減る | `advance_expenses` |

- **「立替」の入り口は2つだけ**（現場従業員用と経営側用）。日報の欄は「立替」ではないので数えない。
- **同じ支払いを2か所に登録しないこと。**
- 日報の経費は**レジの現金から払っている**（2026-08-25 確認）。だから入力時にすぐ引く今の計算が正しい。
- **残課題**：現場スタッフの立替を返金しても手元現金に反映されない
  （`keiri_advance_expenses` に精算フラグが無く、`/cash` も読んでいない）。
  直すときは現金残高の計算に触ることになるので、**必ず事前に相談する**。

### 5-5. 売上はすべて現金（2026-08-25 確認）

手羽屋の売上は**現時点で全額が現金**。PayPay等のキャッシュレス決済は使っていない。
そのため `keiri_account_mapping` の `sales_cashless`（売上・キャッシュレス）は
`is_active = false` にしてある。**行は消していない**ので、将来キャッシュレスを
導入したら `is_active` を `true` に戻すだけで使える。

### 5-6. 次ステージ以降の候補（まだ作っていない）

- 日報データ → 仕訳データへの変換処理
- マネーフォワード クラウド会計の仕訳インポートCSV出力
- 試算表の出力

## 6. 担当スタッフ

- イデ
- じゅん
- かずき
- なぎさ

## 7. 出店場所リスト

- ながやま 鷹尾店
- ながやま 若葉店
- ながやま 三股店
- ながやま 都北店
- ながやま 山田店
- ながやま 志比田店
- マンガ倉庫
- PASIO 高城店
- PASIO 早鈴店
- ニクルの朝市
- まるまる朝市
- BIG OPUS
- Aコープ 木花
- イオンモール
- その他

## 8. 開発者（ユーザー）について

- **名前**: 川畑潤一郎さん
- プログラミング完全未経験
- コードは書かない・ターミナル操作もしない
- Claude Codeへの指示は別のClaude（設計担当）が組み立てて渡してくる
- ブラウザ操作のみ担当（Vercel・Supabaseなどのダッシュボード画面）

## 9. 説明の仕方（超重要）

このプロジェクトのユーザーはプログラミング未経験のため、以下のルールを必ず守ること：

- 技術用語を使うときは必ず噛み砕いた説明を添える
- 専門用語には必ず（カッコ書きで）日常的な例え話を入れる
- コードの変更内容は「何を・なぜ・どうなるか」を小学生にも分かる言葉で説明する

### よく使う用語の言い換え例

| 技術用語 | 分かりやすい説明 |
|---------|---------------|
| GitHub | コードの保管庫（設計図を保存するロッカーのようなもの） |
| Supabase | 業務データの保管庫（売上や日報のデータが入っている倉庫） |
| Vercel | サイトを動かす場所（お店のアプリを世界中に届ける配送センター） |
| git push | 変更をネットに送る（修正した設計図をロッカーにしまう作業） |
| デプロイ | アプリを公開する（修正したお店を実際にオープンする作業） |
| コミット | 変更の記録を残す（作業日誌に「今日はここを直した」と書くこと） |

## 10. 安全ルール（絶対厳守）

以下の操作は**絶対に実行禁止**。必要な場合は必ず事前にユーザーに確認すること。

### 禁止事項1: 全データの削除や復元不可能な操作
- `DROP TABLE`（データベースのテーブルごと削除）
- `DELETE FROM ... WHERE条件なし`（全データ一括削除）
- `git push --force`（履歴を強制上書き）
- `rm -rf`（フォルダごと強制削除）
- `git reset --hard`（変更を全て破棄）

### 禁止事項2: 料金が発生する操作
- 有料APIの新規契約
- 課金プランへのアップグレード
- 外部サービスの有料機能の有効化

**これらが必要な場合は、何をするか・なぜ必要か・どんな影響があるかを分かりやすく説明した上で、ユーザーの許可を得てから実行すること。**
